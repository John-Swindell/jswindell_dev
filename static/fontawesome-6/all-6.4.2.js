/* Font Awesome icons are rendered with the small CSS mask subset in
   layouts/partials/head/extensions.html. This file intentionally shadows the
   theme's full JavaScript bundle while preserving its existing template URL. */
